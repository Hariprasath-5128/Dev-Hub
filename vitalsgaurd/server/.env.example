SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Shared with vitalsgaurd/backend/.env — must be identical on both sides.
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=

# Shared with vitalsgaurd/backend/.env — must be identical on both sides.
INTERNAL_SERVICE_KEY=
